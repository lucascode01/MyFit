# Database (PostgreSQL) - MYfitPersonal
# Docker expõe o Postgres na porta 5433 no host (clinica usa 5432)
POSTGRES_DB=MYfitPersonal
POSTGRES_USER=gym
POSTGRES_PASSWORD=gym_secret

# Django
SECRET_KEY=your-django-secret-key-change-in-production
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# JWT (use long random strings in production)
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# S3 / Object Storage (optional - local storage fallback if not set)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1
AWS_S3_CUSTOM_DOMAIN=
USE_S3=0

# Stripe (assinatura mensal para profissionais)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID=price_...

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000/api
# URL do app (a rota / do backend redireciona para aqui)
FRONTEND_URL=http://localhost:3000
